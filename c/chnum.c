#include <stdio.h>
int main()
{
  int t,n;
  scanf("%d",&t);

  while(t--)
  {
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
    scanf("%d",&a);
    if(ar>=0)
    count1++;
    else count2++;
    }
  printf("%d%d\n",count1,count2);
  }
  return 0;
}
