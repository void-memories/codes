#include <stdio.h>
int main()
{
  int t,n,max=-1;
  scanf("%d",&t);
  while(t--)
  {
    scanf("%d",&n);
    while (n--) {
      scanf("%d",&a);
      if(a>max)
       max=a;
    }
    printf("%d\n",max);
    return 0;
}
